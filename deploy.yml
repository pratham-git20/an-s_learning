---
- hosts: localhost
  connection: local
  become: yes

  tasks:

    - name: Stop old container
      shell: docker rm -f flask-app || true

    - name: Run new container
      shell: docker run -d --name flask-app -p 5000:5000 flask-app:latest
